# FM26 시즌 코치 에이전트

이 문서는 **입구**다. 규칙과 스키마는 여기 복사하지 않고 원본 파일을 그때그때 읽는다.
프로젝트는 하위 폴더 **`fm-agent/`** 안에 있고, 아래 경로는 모두 그 기준이다.

## 세션 시작 시

`coach.md` 를 읽고 그 규칙(정체성·호칭·말투·분석 원칙)을 **세션 내내** 따른다.
이어서 `season_memory.md` 를 읽는다 — 짧고, "지금 상태"가 거기 있다.

**그 외 문서는 필요할 때만 읽는다.** 분석·브리핑에는 위 둘이면 충분하다.

| 언제 | 읽을 것 |
|---|---|
| 경기 JSON 을 직접 만들거나 스키마를 다룰 때 | `schema.md` |
| 도구(HTML·build.ps1)를 고칠 때 | **`DEV.md`** — 마커 구조·BOM 규칙이 거기 있다 |
| 새 기능을 논의하기 전 | `TODO.md` |
| 역할·지침·특성(PPM) 얘기가 나올 때 | `data/` |

## 파일 지도

**표준 표기 (대조한다)** — `teams.json` 팀명 · `competitions.json` 대회명 ·
`comment-rules.json` 자동 코멘트 규칙 · `data/` 역할·지침·PPM 정의(감독이 채운 FM26 자료)

**모델 — 측정값이 아니다**
- `reference-shape.json` 역할별 기대 위치 (감독이 정한 전술 의도)
- `foot-scale.json` 발 칸수(1~6) → 능력치 범위(1~20) 환산 기준

> **데이터와 모델을 혼동하지 않는다.** 이 둘은 게임에서 측정된 값이 아니다.
> 분석할 때 실제 경기 데이터인 것처럼 인용하지 않는다.

**데이터**
| 경로 | 용도 |
|---|---|
| `matches/*.json` | 실제 경기 기록. 승·무·패와 xG 통계의 유일한 근거 |
| `squad/선수_능력치_기록표.csv` | **선수명·포지션·발·특성·능력치의 유일한 기준.** 감독이 직접 관리 |
| `hypotheses/*.json` | 해석을 남기는 곳. 반증 조건(`falsify`) 없는 가설은 만들지 않는다 |
| `tactics/*.json` | 전술 변경 기록. 전후 5경기를 자동 비교하지만 **인과가 아니라 상관** |
| `shapes/*.json` | 경기가 특정되지 않는 패스맵. 통계에 안 들어간다 |
| `digest.md` | build.ps1 이 만드는 **요약본. 분석할 때 이걸 먼저 읽는다** (직접 고치지 않는다) |
| `season_memory.md` | 상태 유지 메모리. **감독이 명시적으로 요청할 때만** 고쳐 쓴다 |
| `screenshots/` | 원본 스크린샷. **절대 삭제 금지** |

**결과물** — `dashboard.html` 시즌 대시보드 · `screenshot-prompt.md` 경기 JSON 지시서 ·
`tactics-board.html` 전술 보드(수석코치 조언 프롬프트 복사)

## 실행

```bash
powershell -ExecutionPolicy Bypass -File "C:\Users\LSH\Desktop\CLAUDE\fm-agent\build.ps1"
```

경고가 나오면 **분석보다 먼저 보고한다.** 대응 수위는 `coach.md` 를 따르고,
표기·중복·수치 오류는 **감독 확인 없이 임의로 고치지 않는다.**

## 상황별

**"경기 넣었어"** — 감독이 다른 AI로 JSON 을 만들어 `matches/` 에 넣은 경우다.
**숫자를 다시 확인하겠다고 스크린샷을 요구하지 않는다.** 바로 build.ps1 을 돌리고,
경고가 나오면 그때만 해당 화면을 요청한다.

**스크린샷을 주면** — `schema.md` 규칙대로 저장한 **뒤에** build.ps1 을 돌린다.

**스쿼드를 갱신하면** — `squad/선수_능력치_기록표.csv` 를 고치고 build.ps1 만 돌리면 된다.
변환 단계는 없다. 이 CSV 가 **선수명의 유일한 기준**이라, 경기 기록에 없는 이름이 들어가면 경고가 뜬다.

**분석 요청을 받으면** — **`matches/` 를 순회하지 않는다.** 순서는:

1. 이번 세션에서 이미 `digest.md` 를 읽었고 그 뒤로 build 를 안 돌렸다면 **다시 읽지 않는다.**
2. 아니면 `digest.md` 하나를 읽는다. 시즌 집계·경기 목록·선수 평점·가설이 다 들어 있다.
3. 감독이 대시보드 **"분석 요청 복사"** 로 붙여넣은 요약이 있으면 그것을 쓴다.
4. 특정 경기의 패스맵·세부 수치가 꼭 필요할 때만 **그 경기 원본 하나**를 연다.

말할 때의 세 층(Fact / Inference / Hypothesis)·표본 기준·열린 가설 확인 순서는 `coach.md` 를 따른다.

## 답변

**짧게.** 수정 이유와 바뀐 부분만 말한다. 표·예시를 습관적으로 붙이지 않는다.
코드를 채팅에 다시 찍지 않는다 — 파일에 적용하고 무엇을 왜 바꿨는지만 말한다.

## 다른 프로젝트

이 저장소에는 **삼국지14 PK 참모** 프로젝트가 `rotk14-agent/` 에 따로 들어 있다.
FM 세션에서는 읽지 않는다. 삼국지 얘기가 나올 때만 `rotk14-agent/GUIDE.md` 로 들어간다.
